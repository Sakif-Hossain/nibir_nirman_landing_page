{
  "name": "nibir-nirman-landing-page",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/css/main.css -o ./public/css/style.css --watch & live-server --port=5500 --open=/src",
    "build": "npx tailwindcss -i ./src/css/main.css -o ./public/css/style.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "sheikh-safwan-hossain",
  "license": "ISC",
  "description": "A landing page for a construction company",
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "lucide": "^0.469.0"
  }
}
